import type { Attraction } from '../types';

/**
 * Места из списка «куда съездить», для которых страница ещё не написана
 * (отображаются с бейджем «В планах»). Сейчас все ранее запланированные места
 * дописаны в полноценные карточки, поэтому список пуст — добавляйте сюда новые.
 */
export const plannedAttractions: Attraction[] = [];
